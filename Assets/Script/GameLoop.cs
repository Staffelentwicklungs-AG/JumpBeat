using UnityEngine;

public class GameLoop : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Get LevelSelection
        // Set AudioSelection
        // Set GameplaySpeed
        // Get Background
        // Get Platforms
        // Get Obstacles
        // or LevelLayout
    }

    // Update is called once per frame
    void Update()
    {
        // Start Run
        // PlayerControls
        // SpawnEnemies from a container that leads outside of players view which enables scalabilty of the level without the need for special spawn points
        // CheckforCollision
        // CountPoints
        // GameOver
        
    }
}
